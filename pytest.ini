[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    error::DeprecationWarning
    error::PendingDeprecationWarning
markers =
    asyncio: marks tests that need an asyncio event loop
